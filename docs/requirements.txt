# CAE 有限元分析软件 Python 依赖清单
# 安装方式：
# 1. 最小运行环境：pip install -r docs/requirements.txt
# 2. 如果你使用 conda，也可以把包名逐个安装到 pyqt6 虚拟环境中

# ---------- 图形界面与三维显示 ----------
PyQt6>=6.4.0
pyvista>=0.43.0
pyvistaqt>=0.11.0
vtk>=9.2.0

# ---------- 建模与网格 ----------
gmsh>=4.11.0

# ---------- C++ / Python 绑定 ----------
pybind11>=2.10.0

# ---------- 数值计算 ----------
numpy>=1.23.0
scipy>=1.9.0

# ---------- 数据处理与绘图 ----------
pandas>=1.5.0
matplotlib>=3.6.0

# ---------- 测试 ----------
pytest>=7.2.0

# ---------- 文档与代码质量 ----------
sphinx>=5.3.0
black>=22.10.0
flake8>=5.0.0

# ---------- 可选开发工具 ----------
jupyter>=1.0.0
ipython>=8.0.0
line_profiler>=4.0.0
memory_profiler>=0.61.0
